---
// Header component for West Valley Christmas Lights
// Clean, responsive design with mobile-first approach
---

<header class="header">
  <div class="header-container">
    <!-- Logo -->
    <a href="/" class="logo-wrapper">
      <img src="/logo.png" alt="West Valley Christmas Lights" class="logo-image" />
    </a>

    <!-- CTA Section - Phone + Button -->
    <div class="header-cta">
      <!-- Hours Text -->
      <div class="hours-text">Open 8am-6pm every day</div>

      <!-- Phone Link - Hidden on mobile, visible on tablet+ -->
      <a href="tel:6022078848" class="phone-link">
        <svg class="phone-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72 12.84 12.84 0 0 0 .7 2.81 2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45 12.84 12.84 0 0 0 2.81.7A2 2 0 0 1 22 16.92z"></path>
        </svg>
        <span class="phone-number">(602) 207-8848</span>
      </a>

      <!-- Reserve Your Spot Button - Always visible -->
      <a href="#quote" class="cta-button">Reserve Your Spot</a>
    </div>
  </div>
</header>

<style>
  /* ===================================
     MOBILE-FIRST BASE STYLES
     =================================== */

  /* Header Container - Sticky position */
  .header {
    position: sticky;
    top: 0;
    z-index: 1000;
    background: #FFFFFF;
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
  }

  .header-container {
    max-width: 1280px;
    margin: 0 auto;
    padding: 0.25rem 1rem;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1rem;
  }

  /* ===================================
     LOGO
     =================================== */
  .logo-wrapper {
    display: flex;
    align-items: center;
    text-decoration: none;
    flex-shrink: 0;
  }

  .logo-image {
    width: 120px;
    height: 120px;
    object-fit: contain;
  }

  /* ===================================
     HOURS TEXT
     =================================== */
  .hours-text {
    font-family: Georgia, 'Times New Roman', serif;
    font-size: 0.75rem; /* Reduced for mobile: 12px */
    color: #16A34A;
    font-weight: 600;
    font-style: italic;
    letter-spacing: 0.01em;
    line-height: 1.1; /* Tighter line-height for mobile */
    max-width: 4.75rem; /* Force wrapping to 2 lines: "Open 8am-6pm" / "every day" */
  }

  /* ===================================
     HEADER CTA SECTION
     Phone link + Reserve Your Spot button
     =================================== */
  .header-cta {
    display: flex;
    align-items: center;
    gap: 0.5rem; /* Reduced gap for mobile to save space */
    flex-shrink: 0;
    margin-left: auto;
  }

  /* Phone Link - Hidden on mobile */
  .phone-link {
    display: none; /* Hidden on mobile, shown on tablet+ */
    align-items: center;
    gap: 0.5rem;
    color: #111827;
    text-decoration: none;
    font-weight: 600;
    transition: color 0.2s ease;
    white-space: nowrap;
  }

  .phone-link:hover {
    color: #DC2626;
  }

  .phone-icon {
    width: 28px;
    height: 28px;
    flex-shrink: 0;
  }

  .phone-number {
    font-size: 1.375rem; /* 22px */
  }

  /* CTA Button - Always visible */
  .cta-button {
    background: #DC2626;
    color: #FFFFFF;
    padding: 0.5rem 0.625rem; /* 8px 10px - reduced for mobile */
    border-radius: 0.5rem; /* 8px */
    text-decoration: none;
    font-weight: 600;
    font-size: 0.9375rem; /* 15px - reduced for mobile to fit */
    transition: background 0.2s ease;
    white-space: nowrap;
    box-shadow: 0 2px 8px rgba(220, 38, 38, 0.3);
  }

  .cta-button:hover {
    background: #B91C1C;
  }

  /* ===================================
     RESPONSIVE BREAKPOINTS
     Mobile-First Strategy
     =================================== */

  /*
     TABLET: 768px - 1023px
     Show: Brand + Nav Links + Phone + Button
     Layout: Everything fits on one line with moderate spacing
  */
  @media (min-width: 768px) {
    .header-container {
      padding: 0.25rem 1.5rem;
      gap: 1.5rem;
    }

    .logo-image {
      width: 140px;
      height: 140px;
    }

    /* Adjust sizing */
    .hours-text {
      font-size: 0.9375rem;
      max-width: none; /* Remove wrap constraint */
      white-space: nowrap; /* Single line on tablet+ */
      line-height: 1.2; /* Reset to normal */
    }

    /* Show phone link */
    .phone-link {
      display: flex;
    }

    .header-cta {
      gap: 1rem; /* Restore gap for tablet+ */
    }

    .cta-button {
      padding: 0.625rem 1.25rem; /* 10px 20px */
      font-size: 1.25rem; /* 20px */
    }
  }

  /*
     DESKTOP: 1024px+
     Show: Brand + Nav Links + Phone + Button
     Layout: Generous spacing, larger font sizes
  */
  @media (min-width: 1024px) {
    .header-container {
      padding: 0.25rem 2rem;
      gap: 2rem;
    }

    .logo-image {
      width: 160px;
      height: 160px;
    }

    .hours-text {
      font-size: 1.0625rem;
    }

    .header-cta {
      gap: 1.5rem;
    }

    .phone-number {
      font-size: 1.5rem; /* 24px */
    }

    .cta-button {
      font-size: 1.5rem; /* 24px */
      padding: 0.75rem 1.5rem; /* 12px 24px */
    }
  }

  /*
     LARGE DESKTOP: 1280px+
     Maximum spacing and optimal layout
  */
  @media (min-width: 1280px) {
    .header-container {
      padding: 0.25rem 2rem;
      gap: 2.5rem;
    }

    .hours-text {
      font-size: 1.125rem;
    }

    .logo-image {
      width: 180px;
      height: 180px;
    }

    .header-cta {
      gap: 2rem;
    }

    .phone-number {
      font-size: 1.625rem; /* 26px */
    }

    .cta-button {
      font-size: 1.625rem; /* 26px */
      padding: 0.875rem 1.75rem; /* 14px 28px */
    }
  }
</style>
