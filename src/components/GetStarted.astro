---
// GetStarted component for West Valley Christmas Lights - Builds confidence and guides visitors to take action
---

<section id="get-started" class="get-started-section">
  <!-- Decorative Background Elements -->
  <div class="section-background" aria-hidden="true">
    <div class="gradient-orb orb-1"></div>
    <div class="gradient-orb orb-2"></div>
  </div>

  <div class="container">
    <div class="section-header">
      <p class="section-label">No Pressure. Zero Obligation.</p>
      <h2 class="section-title">Here's How It Works</h2>
      <p class="section-subtitle">Simple, straightforward, and completely risk-free</p>
    </div>

    <div class="process-steps">
      <div class="step">
        <div class="step-icon-wrapper">
          <div class="step-icon">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72 12.84 12.84 0 0 0 .7 2.81 2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45 12.84 12.84 0 0 0 2.81.7A2 2 0 0 1 22 16.92z"></path>
            </svg>
          </div>
          <div class="step-number">1</div>
        </div>
        <h3 class="step-title">Reserve Your Spot<br>Today</h3>
        <p class="step-description">Call us or fill out the form at the top of the page. This is completely free and there's zero obligation. We're just securing a time slot on our schedule for you.</p>
      </div>

      <div class="step">
        <div class="step-icon-wrapper">
          <div class="step-icon">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="M9 11l3 3L22 4"></path>
              <path d="M21 12v7a2 2 0 01-2 2H5a2 2 0 01-2-2V5a2 2 0 012-2h11"></path>
            </svg>
          </div>
          <div class="step-number">2</div>
        </div>
        <h3 class="step-title">Free Consultation & Custom Quote</h3>
        <p class="step-description">We'll come to your property, assess your home's unique layout, discuss your vision, and provide a detailed quote. No high-pressure sales tactics. Just honest pricing.</p>
      </div>

      <div class="step">
        <div class="step-icon-wrapper">
          <div class="step-icon">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="M5 12h14"></path>
              <path d="M12 5l7 7-7 7"></path>
            </svg>
          </div>
          <div class="step-number">3</div>
        </div>
        <h3 class="step-title">You Decide. No Pressure.</h3>
        <p class="step-description">Review the quote on your own time. If you love it, we'll schedule your installation. If not, no worries whatsoever. The consultation and quote are 100% free.</p>
      </div>
    </div>

    <!-- CTA Spotlight Section -->
    <div class="cta-spotlight">
      <div class="cta-spotlight-inner">
        <div class="spotlight-header">
          <h3 class="spotlight-title">Ready to Get Your Free Quote?</h3>
          <p class="spotlight-subtitle">Choose the fastest way to reach us - no strings attached</p>
        </div>

        <div class="cta-options">
          <a href="tel:6022078848" class="cta-phone">
            <div class="cta-phone-icon">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72 12.84 12.84 0 0 0 .7 2.81 2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45 12.84 12.84 0 0 0 2.81.7A2 2 0 0 1 22 16.92z"></path>
              </svg>
            </div>
            <div class="cta-phone-content">
              <span class="cta-phone-label">Call or Text Now</span>
              <span class="cta-phone-number">(602) 207-8848</span>
            </div>
            <div class="cta-phone-badge">Fastest Response</div>
          </a>

          <div class="cta-divider">
            <div class="divider-line"></div>
            <span class="divider-text">OR</span>
            <div class="divider-line"></div>
          </div>

          <a href="#quote" class="scroll-to-form-btn">
            <div class="form-btn-icon">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <path d="M18 15l-6-6-6 6"></path>
              </svg>
            </div>
            <div class="form-btn-content">
              <span class="form-btn-label">Fill Out Quick Form</span>
              <span class="form-btn-sublabel">Jump to form at top</span>
            </div>
          </a>
        </div>

        <!-- Trust Badges -->
        <div class="trust-badges">
          <div class="trust-badge">
            <div class="trust-badge-icon">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"></path>
              </svg>
            </div>
            <span class="trust-badge-text">No Obligation</span>
          </div>
          <div class="trust-badge">
            <div class="trust-badge-icon">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"></path>
                <polyline points="22 4 12 14.01 9 11.01"></polyline>
              </svg>
            </div>
            <span class="trust-badge-text">100% Free Quote</span>
          </div>
          <div class="trust-badge">
            <div class="trust-badge-icon">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"></path>
                <circle cx="12" cy="7" r="4"></circle>
              </svg>
            </div>
            <span class="trust-badge-text">Local & Trusted</span>
          </div>
        </div>
      </div>
    </div>

    <!-- Why Reserve Section -->
    <div class="why-reserve-section">
      <div class="why-reserve-card">
        <h3 class="why-reserve-title">Why Reserve Your Spot Now?</h3>
        <div class="why-reserve-grid">
          <div class="reserve-item">
            <div class="reserve-icon">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <rect x="3" y="4" width="18" height="18" rx="2" ry="2"></rect>
                <line x1="16" y1="2" x2="16" y2="6"></line>
                <line x1="8" y1="2" x2="8" y2="6"></line>
                <line x1="3" y1="10" x2="21" y2="10"></line>
              </svg>
            </div>
            <div class="reserve-content">
              <h4 class="reserve-subtitle">Schedule Fills Up Fast</h4>
              <p class="reserve-description">Holiday season is our busiest time. Booking early gives you priority access and more installation date options.</p>
            </div>
          </div>

          <div class="reserve-item">
            <div class="reserve-icon">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <polyline points="20 6 9 17 4 12"></polyline>
              </svg>
            </div>
            <div class="reserve-content">
              <h4 class="reserve-subtitle">Maximum Flexibility</h4>
              <p class="reserve-description">Reserve now, decide later. You get to review pricing and make an informed decision on your own timeline.</p>
            </div>
          </div>

          <div class="reserve-item">
            <div class="reserve-icon">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <path d="M12 2v20M17 5H9.5a3.5 3.5 0 0 0 0 7h5a3.5 3.5 0 0 1 0 7H6"></path>
              </svg>
            </div>
            <div class="reserve-content">
              <h4 class="reserve-subtitle">Transparent Pricing</h4>
              <p class="reserve-description">Get detailed quotes with no hidden fees. See exactly what you're paying for before making any commitment.</p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .get-started-section {
    position: relative;
    background: linear-gradient(180deg, #F9FAFB 0%, #FFFFFF 50%, #F3F4F6 100%);
    padding: 5rem 0;
    overflow: hidden;
  }

  /* Decorative Background */
  .section-background {
    position: absolute;
    inset: 0;
    pointer-events: none;
    z-index: 0;
  }

  .gradient-orb {
    position: absolute;
    border-radius: 50%;
    filter: blur(80px);
    opacity: 0.15;
    animation: float 20s ease-in-out infinite;
  }

  .orb-1 {
    width: 500px;
    height: 500px;
    background: radial-gradient(circle, #DC2626 0%, transparent 70%);
    top: -200px;
    right: -100px;
    animation-delay: 0s;
  }

  .orb-2 {
    width: 400px;
    height: 400px;
    background: radial-gradient(circle, #16A34A 0%, transparent 70%);
    bottom: -150px;
    left: -50px;
    animation-delay: 10s;
  }

  @keyframes float {
    0%, 100% {
      transform: translate(0, 0) scale(1);
    }
    33% {
      transform: translate(30px, -30px) scale(1.1);
    }
    66% {
      transform: translate(-20px, 20px) scale(0.9);
    }
  }

  .container {
    position: relative;
    z-index: 1;
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 1.5rem;
  }

  /* Header */
  .section-header {
    text-align: center;
    margin-bottom: 4rem;
  }

  .section-label {
    font-size: 0.875rem;
    font-weight: 700;
    color: #16A34A;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    margin-bottom: 0.75rem;
  }

  .section-title {
    font-size: clamp(2rem, 5vw, 3.5rem);
    font-weight: 800;
    color: #111827;
    margin-bottom: 1rem;
    line-height: 1.1;
  }

  .section-subtitle {
    font-size: clamp(1rem, 2vw, 1.25rem);
    color: #6B7280;
    max-width: 600px;
    margin: 0 auto;
    line-height: 1.6;
  }

  /* Process Steps */
  .process-steps {
    display: grid;
    grid-template-columns: 1fr;
    gap: 1.5rem;
    margin-bottom: 5rem;
  }

  .step {
    background: white;
    padding: 2rem;
    border-radius: 1.25rem;
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.06);
    border: 1px solid rgba(0, 0, 0, 0.05);
    transition: all 0.3s ease;
  }

  .step:hover {
    transform: translateY(-4px);
    box-shadow: 0 12px 35px rgba(0, 0, 0, 0.12);
    border-color: rgba(220, 38, 38, 0.2);
  }

  .step-icon-wrapper {
    position: relative;
    display: inline-block;
    margin-bottom: 1.5rem;
  }

  .step-icon {
    width: 64px;
    height: 64px;
    background: linear-gradient(135deg, #DC2626 0%, #B91C1C 100%);
    color: white;
    border-radius: 1.25rem;
    display: flex;
    align-items: center;
    justify-content: center;
    box-shadow: 0 6px 20px rgba(220, 38, 38, 0.35);
  }

  .step-icon svg {
    width: 32px;
    height: 32px;
  }

  .step-number {
    position: absolute;
    top: -10px;
    right: -10px;
    width: 32px;
    height: 32px;
    background: #16A34A;
    color: white;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 0.875rem;
    font-weight: 700;
    box-shadow: 0 4px 12px rgba(22, 163, 74, 0.4);
    border: 3px solid white;
  }

  .step-title {
    font-size: 1.5rem;
    font-weight: 700;
    color: #111827;
    margin-bottom: 0.75rem;
  }

  .step-description {
    font-size: 1rem;
    line-height: 1.7;
    color: #6B7280;
  }

  /* CTA Spotlight Section */
  .cta-spotlight {
    margin-bottom: 4rem;
  }

  .cta-spotlight-inner {
    background: linear-gradient(135deg, #1F2937 0%, #111827 100%);
    padding: 3rem 2rem;
    border-radius: 1.5rem;
    box-shadow: 0 10px 40px rgba(0, 0, 0, 0.2);
    position: relative;
    overflow: hidden;
  }

  .cta-spotlight-inner::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 4px;
    background: linear-gradient(90deg, #DC2626 0%, #16A34A 100%);
  }

  .spotlight-header {
    text-align: center;
    margin-bottom: 3rem;
  }

  .spotlight-title {
    font-size: clamp(1.75rem, 4vw, 2.25rem);
    font-weight: 800;
    color: white;
    margin-bottom: 0.75rem;
    line-height: 1.2;
  }

  .spotlight-subtitle {
    font-size: 1.125rem;
    color: #D1D5DB;
    line-height: 1.6;
  }

  .cta-options {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
    margin-bottom: 3rem;
  }

  /* Enhanced Phone CTA */
  .cta-phone {
    position: relative;
    display: flex;
    align-items: center;
    gap: 1.25rem;
    background: linear-gradient(135deg, #DC2626 0%, #B91C1C 100%);
    padding: 1.5rem 1.75rem;
    border-radius: 1rem;
    text-decoration: none;
    transition: all 0.3s ease;
    box-shadow: 0 8px 25px rgba(220, 38, 38, 0.4);
    border: 2px solid rgba(255, 255, 255, 0.1);
  }

  .cta-phone:hover {
    background: linear-gradient(135deg, #B91C1C 0%, #991B1B 100%);
    transform: translateY(-3px);
    box-shadow: 0 12px 35px rgba(220, 38, 38, 0.5);
  }

  .cta-phone-icon {
    flex-shrink: 0;
    width: 56px;
    height: 56px;
    background: rgba(255, 255, 255, 0.2);
    border-radius: 0.75rem;
    display: flex;
    align-items: center;
    justify-content: center;
    color: white;
    backdrop-filter: blur(10px);
  }

  .cta-phone-icon svg {
    width: 28px;
    height: 28px;
  }

  .cta-phone-content {
    flex: 1;
    display: flex;
    flex-direction: column;
    gap: 0.25rem;
  }

  .cta-phone-label {
    font-size: 0.875rem;
    font-weight: 600;
    color: rgba(255, 255, 255, 0.9);
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  .cta-phone-number {
    font-size: 1.625rem;
    font-weight: 800;
    color: white;
    letter-spacing: -0.02em;
  }

  .cta-phone-badge {
    position: absolute;
    top: -12px;
    right: 1rem;
    background: #16A34A;
    color: white;
    padding: 0.375rem 0.875rem;
    border-radius: 1rem;
    font-size: 0.6875rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    box-shadow: 0 4px 12px rgba(22, 163, 74, 0.4);
  }

  /* Enhanced Divider */
  .cta-divider {
    display: flex;
    align-items: center;
    gap: 1rem;
  }

  .divider-line {
    flex: 1;
    height: 1px;
    background: linear-gradient(90deg, transparent 0%, #374151 50%, transparent 100%);
  }

  .divider-text {
    font-size: 0.875rem;
    font-weight: 700;
    color: #9CA3AF;
    letter-spacing: 0.1em;
  }

  /* Enhanced Form Button */
  .scroll-to-form-btn {
    display: flex;
    align-items: center;
    gap: 1.25rem;
    background: transparent;
    border: 2px solid #16A34A;
    color: #16A34A;
    padding: 1.5rem 1.75rem;
    border-radius: 1rem;
    font-size: 1.125rem;
    font-weight: 700;
    cursor: pointer;
    transition: all 0.3s ease;
    font-family: inherit;
    width: 100%;
  }

  .scroll-to-form-btn:hover {
    background: #16A34A;
    color: white;
    transform: translateY(-3px);
    box-shadow: 0 12px 35px rgba(22, 163, 74, 0.4);
  }

  .form-btn-icon {
    flex-shrink: 0;
    width: 40px;
    height: 40px;
    background: rgba(22, 163, 74, 0.15);
    border-radius: 0.5rem;
    display: flex;
    align-items: center;
    justify-content: center;
    transition: all 0.3s ease;
  }

  .scroll-to-form-btn:hover .form-btn-icon {
    background: rgba(255, 255, 255, 0.2);
  }

  .form-btn-icon svg {
    width: 20px;
    height: 20px;
  }

  .form-btn-content {
    flex: 1;
    display: flex;
    flex-direction: column;
    gap: 0.25rem;
    text-align: left;
  }

  .form-btn-label {
    font-size: 1.125rem;
    font-weight: 700;
  }

  .form-btn-sublabel {
    font-size: 0.875rem;
    font-weight: 500;
    opacity: 0.8;
  }

  /* Enhanced Trust Badges */
  .trust-badges {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1.5rem;
    padding-top: 2.5rem;
    border-top: 1px solid rgba(255, 255, 255, 0.1);
  }

  .trust-badge {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.75rem;
    text-align: center;
  }

  .trust-badge-icon {
    width: 56px;
    height: 56px;
    background: rgba(22, 163, 74, 0.15);
    border-radius: 0.75rem;
    display: flex;
    align-items: center;
    justify-content: center;
    color: #16A34A;
  }

  .trust-badge-icon svg {
    width: 32px;
    height: 32px;
  }

  .trust-badge-text {
    font-size: 1.0625rem;
    font-weight: 600;
    color: #D1D5DB;
    line-height: 1.4;
  }

  /* Why Reserve Section */
  .why-reserve-section {
    margin-top: 4rem;
  }

  .why-reserve-card {
    background: white;
    padding: 3rem 2rem;
    border-radius: 1.5rem;
    box-shadow: 0 8px 30px rgba(0, 0, 0, 0.08);
    border: 1px solid rgba(0, 0, 0, 0.05);
  }

  .why-reserve-title {
    font-size: 1.875rem;
    font-weight: 800;
    color: #111827;
    text-align: center;
    margin-bottom: 3rem;
  }

  .why-reserve-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 2.5rem;
  }

  .reserve-item {
    display: flex;
    gap: 1.5rem;
    align-items: flex-start;
  }

  .reserve-icon {
    flex-shrink: 0;
    width: 56px;
    height: 56px;
    background: linear-gradient(135deg, #FEE2E2 0%, #FECACA 100%);
    border-radius: 1rem;
    display: flex;
    align-items: center;
    justify-content: center;
    color: #DC2626;
  }

  .reserve-icon svg {
    width: 28px;
    height: 28px;
  }

  .reserve-content {
    flex: 1;
  }

  .reserve-subtitle {
    font-size: 1.25rem;
    font-weight: 700;
    color: #111827;
    margin-bottom: 0.5rem;
  }

  .reserve-description {
    font-size: 1rem;
    line-height: 1.7;
    color: #6B7280;
    margin: 0;
  }

  /* Tablet */
  @media (min-width: 768px) {
    .get-started-section {
      padding: 6rem 0;
    }

    .process-steps {
      grid-template-columns: repeat(3, 1fr);
      gap: 2rem;
      margin-bottom: 6rem;
    }

    .step {
      padding: 2.5rem;
    }

    .cta-spotlight-inner {
      padding: 3.5rem 3rem;
    }

    .trust-badges {
      gap: 2rem;
    }

    .trust-badge-icon {
      width: 64px;
      height: 64px;
    }

    .trust-badge-icon svg {
      width: 36px;
      height: 36px;
    }

    .trust-badge-text {
      font-size: 1.125rem;
    }

    .why-reserve-card {
      padding: 3.5rem 3rem;
    }

    .why-reserve-grid {
      gap: 3rem;
    }
  }

  /* Desktop */
  @media (min-width: 1024px) {
    .get-started-section {
      padding: 7rem 0;
    }

    .section-header {
      margin-bottom: 5rem;
    }

    .cta-spotlight-inner {
      padding: 4rem 4rem 3.5rem;
    }

    .cta-options {
      flex-direction: row;
      align-items: stretch;
      gap: 2rem;
    }

    .cta-phone {
      flex: 1;
    }

    .cta-divider {
      flex-direction: column;
      justify-content: center;
    }

    .divider-line {
      display: none;
    }

    .scroll-to-form-btn {
      flex: 1;
    }

    .why-reserve-grid {
      grid-template-columns: repeat(3, 1fr);
      gap: 2.5rem;
    }

    .reserve-item {
      flex-direction: column;
      text-align: center;
      align-items: center;
    }

    .reserve-icon {
      margin-bottom: 0.5rem;
    }
  }

  /* Extra Large Screens */
  @media (min-width: 1280px) {
    .cta-phone-number {
      font-size: 1.875rem;
    }

    .form-btn-label {
      font-size: 1.25rem;
    }
  }
</style>
