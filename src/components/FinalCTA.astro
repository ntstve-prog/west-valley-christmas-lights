---
// Final CTA component for West Valley Christmas Lights - Simple, impactful last-chance opportunity
---

<section class="final-cta-section">
  <div class="final-cta-container">
    <div class="final-cta-content">
      <h2 class="final-cta-headline">Ready to Light Up Your Home?</h2>
      <p class="final-cta-subheadline">Get your free quote today. No obligation, no pressure.</p>

      <div class="final-cta-buttons">
        <a href="tel:6022078848" class="final-cta-phone-btn">
          <svg class="phone-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72 12.84 12.84 0 0 0 .7 2.81 2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45 12.84 12.84 0 0 0 2.81.7A2 2 0 0 1 22 16.92z"></path>
          </svg>
          <span>Call (602) 207-8848</span>
        </a>

        <button onclick="window.scrollTo({top: 0, behavior: 'smooth'})" class="final-cta-form-btn">
          <svg class="up-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <path d="M18 15l-6-6-6 6"></path>
          </svg>
          <span>Fill Out Form</span>
        </button>
      </div>
    </div>
  </div>
</section>

<style>
  .final-cta-section {
    background: linear-gradient(135deg, #111827 0%, #1F2937 100%);
    padding: 4rem 0;
    position: relative;
    overflow: hidden;
  }

  /* Subtle decorative gradient */
  .final-cta-section::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 3px;
    background: linear-gradient(90deg, #DC2626 0%, #16A34A 50%, #DC2626 100%);
  }

  .final-cta-container {
    max-width: 900px;
    margin: 0 auto;
    padding: 0 1.5rem;
  }

  .final-cta-content {
    text-align: center;
  }

  .final-cta-headline {
    font-size: clamp(2rem, 5vw, 3rem);
    font-weight: 800;
    color: #fff;
    margin-bottom: 0.75rem;
    line-height: 1.1;
  }

  .final-cta-subheadline {
    font-size: clamp(1.125rem, 2vw, 1.5rem);
    color: #D1D5DB;
    margin-bottom: 2.5rem;
    line-height: 1.5;
  }

  .final-cta-buttons {
    display: flex;
    flex-direction: column;
    gap: 1rem;
    align-items: center;
  }

  /* Phone button - primary red CTA */
  .final-cta-phone-btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 0.75rem;
    background: linear-gradient(135deg, #DC2626 0%, #B91C1C 100%);
    color: #fff;
    padding: 1.25rem 2.5rem;
    border-radius: 0.75rem;
    text-decoration: none;
    font-weight: 700;
    font-size: 1.125rem;
    transition: all 0.3s ease;
    box-shadow: 0 8px 25px rgba(220, 38, 38, 0.4);
    border: 2px solid rgba(255, 255, 255, 0.1);
    min-width: 280px;
  }

  .final-cta-phone-btn:hover {
    background: linear-gradient(135deg, #B91C1C 0%, #991B1B 100%);
    transform: translateY(-3px);
    box-shadow: 0 12px 35px rgba(220, 38, 38, 0.5);
  }

  .phone-icon {
    width: 24px;
    height: 24px;
    flex-shrink: 0;
  }

  /* Form button - secondary green outline */
  .final-cta-form-btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 0.75rem;
    background: transparent;
    color: #16A34A;
    padding: 1.25rem 2.5rem;
    border-radius: 0.75rem;
    border: 2px solid #16A34A;
    font-weight: 700;
    font-size: 1.125rem;
    cursor: pointer;
    transition: all 0.3s ease;
    font-family: inherit;
    min-width: 280px;
  }

  .final-cta-form-btn:hover {
    background: #16A34A;
    color: #fff;
    transform: translateY(-3px);
    box-shadow: 0 12px 35px rgba(22, 163, 74, 0.4);
  }

  .up-icon {
    width: 24px;
    height: 24px;
    flex-shrink: 0;
  }

  /* Tablet */
  @media (min-width: 768px) {
    .final-cta-section {
      padding: 5rem 0;
    }

    .final-cta-buttons {
      flex-direction: row;
      gap: 1.5rem;
      justify-content: center;
    }

    .final-cta-phone-btn,
    .final-cta-form-btn {
      min-width: 240px;
    }
  }

  /* Desktop */
  @media (min-width: 1024px) {
    .final-cta-section {
      padding: 6rem 0;
    }

    .final-cta-subheadline {
      margin-bottom: 3rem;
    }

    .final-cta-buttons {
      gap: 2rem;
    }
  }
</style>
